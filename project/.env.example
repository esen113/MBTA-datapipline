# ========= GCP 基本配置 =========
GCP_PROJECT=your-gcp-project-id
GCP_LOCATION=US
GCS_BUCKET=your-data-bucket              # 需预先存在（或用 gsutil 创建）
BQ_DATASET=transport                     # BigQuery 数据集，会自动创建（需要权限）

# ========= 母鸭（MotherDuck）配置 =========
# 两种方式择一：
# 1) 推荐：设置 motherduck_token（小写键名是官方建议）
motherduck_token=mdp_xxx_your_token_here
# 2) 或者 MOTHERDUCK_TOKEN（代码同时兼容）
# MOTHERDUCK_TOKEN=mdp_xxx_your_token_here
MD_DATABASE=bb_mbta                      # 在 MotherDuck 中使用的数据库名

# ========= 数据源 =========
BLUEBIKES_S3_BASE=https://s3.amazonaws.com/hubway-data
BLUEBIKES_START_YYYYMM=202301            # 首次回填的起始月份（含）；之后每周只处理新月
BLUEBIKES_MAX_MONTHS_PER_RUN=2           # 每次最多处理多少个月（避免一次性扫太多）

MBTA_GTFS_URL=https://cdn.mbta.com/MBTA_GTFS.zip
# 若未来需要调用 MBTA V3 API（可选）：https://api-v3.mbta.com/ 申请 key
MBTA_API_KEY=                            # 可留空（本方案默认只抓静态 GTFS）

# ========= 其它 =========
TZ=UTC
